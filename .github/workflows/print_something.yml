name: Print Something
on: workflow_call

jobs:
  print-job:
    name: print payload to screen
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{github.sha}}
      - run: echo ${{github.sha}}
